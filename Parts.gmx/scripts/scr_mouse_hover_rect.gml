///scr_mouse_hover_rect(x, y, width, height)

if mouse_x > argument0 && mouse_y > argument1 && mouse_x < argument0 + argument2 && mouse_y < argument1 + argument3
{
    return true;
}
else
{
    return false;
}
